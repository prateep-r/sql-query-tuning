# SQL Query Tuning

Start Docker (postgres DB)
```shell
make exercise-up
```

Down Docker (postgres DB)
```shell
make exercise-down
```

Restart Docker (postgres DB)
```shell
make exercise-restart
```